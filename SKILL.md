---
name: extract-book-skill
description: 将书名、封面、目录、电子书或书摘融合为内容丰富、仅适配手机屏幕的自包含 HTML；支持“吃透一本书”的深度萃取卡、金句摘抄卡，以及可直接交付的内置演示内容。适用于拆书、深度读书笔记、书籍知识卡和内容摘抄；不用于简单书单或一句话书评。
---

# 吃透一本书

根据用户意图输出两种不同产物：完整拆书使用横向多页读书卡；明确的金句或内容摘抄使用竖向摘句卡。最终主交付物都是仅面向手机屏幕、占满手机视口的统一样式单文件 HTML，不提供桌面版布局，也不是长篇聊天回复。

## 内置演示内容

用户明确要求以下演示内容时，按名称直接交付对应 HTML；不重新萃取、渲染或改写它：

- “演示深度萃取”：交付 [references/demo-deliberate-practice-v2.html](references/demo-deliberate-practice-v2.html)，即 9 页《刻意练习》深度萃取 demo。
- “演示金句摘抄”：交付 [references/demo-quote-cards.html](references/demo-quote-cards.html)，即上下滑动的 8 条金句摘抄 demo。
- 泛称“演示内容”“输出演示”或“查看演示成品”：交付 [references/demo-deliberate-practice-v2.html](references/demo-deliberate-practice-v2.html)。

若用户在索取演示内容时同时提出明确的样式或文案修改，则以该演示文件为起点，只按所述范围修改后再交付。

深度萃取 demo 仅保留 [references/demo-deliberate-practice-v2.html](references/demo-deliberate-practice-v2.html)，它也是默认 demo。内容路径为“问题 → 区别 → 方法 → 行动 → 收束”，并带有克制的入场与封面浮动动效。

## 模式路由

先判断用户要哪种产物，不要把两种模式混在一个 HTML 中：

- **演示深度萃取**：直接交付 9 页《刻意练习》V2。
- **演示金句摘抄**：直接交付上下滑动的 8 条金句摘抄 demo。
- **演示内容**：用户只要求查看或输出泛用演示时，直接交付 9 页《刻意练习》V2。
- **模式 A｜深度萃取卡**：用户说“吃透一本书、拆书、深度阅读、知识萃取、做完整读书卡”等，执行完整知识萃取流程。
- **模式 B｜金句摘抄卡**：用户明确说“摘抄金句、摘录内容、提取原句、做金句卡、把这些句子做成卡片”等，只提取可核对的原文或用户提供的摘录，不执行模型、行动、反常识等完整拆书模块。
- 同时要求两种产物时分别生成两个 HTML，不把金句页硬塞进深度萃取卡。

## 必读资源

开始萃取前：

1. 两种模式都先阅读 [references/content-policy.md](references/content-policy.md)。
2. 模式 A 阅读 [references/content-schema.md](references/content-schema.md) 和 [references/card-design.md](references/card-design.md)，使用 [references/reading-card-template.html](references/reading-card-template.html)。
3. 模式 B 阅读 [references/quote-card-schema.md](references/quote-card-schema.md)，使用 [references/reading-quote-cards-template.html](references/reading-quote-cards-template.html)；原始视觉参考保存在 [references/reading-quote-cards-reference.html](references/reading-quote-cards-reference.html)。
4. 分别使用对应渲染脚本；不要每次重新设计 HTML。

## 输入与启动

支持书名、封面、目录、电子书、书影或书摘。拿到可识别书名后直接完成，不在流程中反复询问。

如果没有任何可识别的书名或书籍材料，只问一个必要问题：要处理哪本书。不要自行指定默认书目。

## 模式 A｜深度萃取卡

### 1. 建立理解底稿

先盘点用户提供的材料；需要补齐全书结构、版本信息或易变化事实时，再检索出版社介绍、官方目录、作者资料、正版试读或可信书评。允许使用模型已有知识补充核心概念、背景脉络、典型例子、方法边界和应用理解，不因缺少完整正文而只做目录复述。

事实证据不是深度卡的首要目标，优先保证内容完整、解释清楚和可读。但不要编造逐字引文、页码、统计数字、作者经历或过度精确的案例细节。内部判断哪些是书中主旨、常识性补充和应用延伸，成品中不显示这套判断过程。

### 2. 萃取知识

根据资料充分度，从下列模块中选择实际有价值的内容，不要求每本书机械凑齐相同页数：

- 书籍定位：是什么、不是什么、适合谁、可以获得哪些可观察变化。
- 现实问题：书回应的具体生活、学习或工作场景。
- 核心知识单元：核心内容、运作机制、适用卡点、微案例、使用误区。
- 逻辑剖析：为什么懂了做不到、因果链和反例。
- 模型工具：基于书中内容与通识知识设计的可执行框架。
- 场景迁移：目标、适用知识单元和下一步动作。
- 行动转化：时间限制、具体动作、可验证完成标志和复盘。
- 反常识：认知调整、小步尝试和反馈标准。
- 典型案例：优先解释案例揭示的机制，不堆砌人物和数字。
- 方法边界：书中观点在哪些场景有效，在哪些场景需要调整。
- 短引文或概念：逐字引文只在能够核对时使用；否则写成自然的核心表达。
- 延伸理解：可以融入相关心理学、学习科学、管理或生活经验，使全书知识形成网络。
- 终极凝结：核心观点、成长价值和四个四字短句。

不要生成“一句狠话”、口号改造、社群话题、互动引导或后续预告。

### 3. 编排卡片

页数由内容决定。每页聚焦一个核心意思，优先控制在首屏内；为了保持字号与完整表达，内容较多时允许页面内部向下滚动。只有当单页主题过多或滚动明显过长时才拆页，不为强行一屏而压缩字号或删掉关键解释。

- 第 1 页固定为演示型封面总览，可出现封面、书名、作者、核心主张和三项阅读收获。首屏必须在不翻页时就说清“这本书解决什么问题、读完能带走什么”，不能只放书籍身份和一段短简介。
- 第 2 页以后不得再显示封面、书名或作者。
- 表格改写为纵向卡片或拆成多页。
- 不显示“原文、转述、篇名、推读、未查证”等标签，不显示章节来源、链接或证据页。
- 将终极凝结放在接近结尾的位置；最后一页应是内容收束、行动计划或回顾，而不是来源说明。

严格按照 [references/content-schema.md](references/content-schema.md) 生成数据。不要把书名复制进后续页的标题、正文或页脚。

### 4. 渲染统一 HTML

把结构化数据写入临时 JSON，然后运行：

```bash
python3 scripts/render_book_cards.py book-data.json output.html
```

若提供本地封面图片，在 JSON 的 `book.cover_image` 中写入路径；渲染器会内嵌为 data URI。不要在最终 HTML 中依赖外部字体、脚本、样式或图片。

### 5. 验证

交付前必须检查：

- HTML 可直接打开，且只有一个文件依赖。
- 页面占满手机视口，不出现桌面端居中模型、固定桌面宽度或外层装饰框。
- 第 1 页有书籍身份；后续页没有封面、书名或作者。
- 页面数量与数据一致，底部始终显示“当前页 / 总页数 · 当前栏目”；上一页、下一页、左右滑动和左右方向键可用。
- 页面首先显示清晰的阅读起点；较长页面可以自然向下滚动，不能出现文字裁切、导航遮挡正文或滚动失效。
- 页面上没有证据标签、章节出处、网址或来源页。
- 内容没有把模型补充伪装成逐字原话，也没有虚构精确事实。
- 浏览器控制台无错误。

## 模式 B｜金句摘抄卡

### 1. 只提取可核对文字

用户提供书摘、电子书、图片或正文时，以这些材料为准逐字摘录。只有书名时，检索官方试读、出版社页面或其他可核验文本；模型记忆不能充当原文。

没有足够可核对句子时，按实际数量输出，不设置最低条数；不得为了凑页数写出疑似原话。

### 2. 选择适合成卡的摘录

- 每屏一条原句或一个连续短段落。
- 保留原有标点与措辞，不做口号改造。
- 删除只有脱离上下文才显得“漂亮”、但会歪曲原意的句子。
- 每条必须记录作者或章节等短来源，以及更精确的章节、页码、链接或“用户提供摘录”定位。
- 按原书顺序排列；用户指定主题时按主题筛选，但不打乱同一段内部顺序。

### 3. 渲染摘句卡

严格按照 [references/quote-card-schema.md](references/quote-card-schema.md) 生成 JSON，然后运行：

```bash
python3 scripts/render_quote_cards.py quote-data.json quotes.html
```

渲染器会按照文字长度自动选择大、中、小字号，生成动态页数、上下滑动、进度条、页码与阅读位置记忆。不要把深度萃取卡的封面面板、知识模块或行动建议加入摘句卡。

### 4. 验证摘句卡

- 每条摘录与输入材料或可靠来源逐字一致。
- 每条都有来源定位。
- 长摘录不裁切，短摘录不过度缩小。
- 上下滑动、方向键、进度条和计数器正常。
- HTML 单文件、零外部依赖，控制台无错误。
- 页面占满手机视口，不生成桌面端专用布局。

## 交付

默认只把所选模式生成的 HTML 作为主交付物，并简要说明页数和内容结构。除非用户要求，不额外输出长篇 Markdown 读书笔记，也不向用户展示内部资料判断过程。若用户同时明确要求深度萃取和金句摘抄，交付两个独立 HTML。
